<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.atguigu.invest.MainActivity"
    android:orientation="vertical">
    <FrameLayout
        android:id="@+id/fl_main"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"/>
    <RadioGroup
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:orientation="horizontal"
        android:id="@+id/rg_main">
        <RadioButton
            android:id="@+id/rb_home"
            android:layout_marginTop="5dp"
            style="@style/MainButtonStyle"
            android:checked="true"
            android:drawableTop="@drawable/rb_home_selector"
            android:text="首页"/>
        <RadioButton
            android:id="@+id/rb_invest"
            android:layout_marginTop="5dp"
            style="@style/MainButtonStyle"
            android:drawableTop="@drawable/rb_invest_selector"
            android:text="投资"/>
        <RadioButton
            android:id="@+id/rb_propert"
            android:layout_marginTop="5dp"
            style="@style/MainButtonStyle"
            android:drawableTop="@drawable/rb_propert_selector"
            android:text="资产"/>
        <RadioButton
            android:id="@+id/rb_more"
            android:layout_marginTop="5dp"
            style="@style/MainButtonStyle"
            android:drawableTop="@drawable/rb_more_selector"
            android:text="更多"/>
    </RadioGroup>


</LinearLayout>
